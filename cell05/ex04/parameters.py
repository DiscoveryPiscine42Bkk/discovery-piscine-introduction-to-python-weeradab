#!/usr/bin/env python3
import sys  #which provides access to command-line arguments

# Count of arguments passed (excluding the script name)
para_count = len(sys.argv) - 1
print(f"Number of parameters: {para_count}")
